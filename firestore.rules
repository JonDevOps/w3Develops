
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }
    
    function isGroupMember(groupId) {
      return request.auth != null && get(/databases/$(database)/documents/studyGroups/$(groupId)).data.memberIds.hasAny([request.auth.uid]);
    }
    
    function isBookClubMember(bookClubId) {
      return request.auth != null && get(/databases/$(database)/documents/bookClubs/$(bookClubId)).data.memberIds.hasAny([request.auth.uid]);
    }

    function isGroupProjectMember(groupProjectId) {
       return request.auth != null && get(/databases/$(database)/documents/groupProjects/$(groupProjectId)).data.memberIds.hasAny([request.auth.uid]);
    }

    function isAddingToList(list, newList) {
      return newList.size() == list.size() + 1 && newList.hasAll(list);
    }

    function isRemovingFromList(list, newList) {
      return newList.size() == list.size() - 1 && list.hasAll(newList);
    }

    match /users/{userId} {
      allow read: if true;
      allow create: if request.auth.uid == userId;
      allow delete: if isOwner(userId);
      allow update: if isOwner(userId) || (
          // Allow another user to add/remove THEMSELVES from THIS user's followers list
          request.auth != null && (
            (isAddingToList(resource.data.followers, request.resource.data.followers) && request.resource.data.followers[request.resource.data.followers.size() - 1] == request.auth.uid) ||
            (isRemovingFromList(resource.data.followers, request.resource.data.followers) && !request.resource.data.followers.hasAny([request.auth.uid]))
          )
        );
        
      // Allow user to update their own fields
      allow write: if isOwner(userId) && (
        (
          request.resource.data.keys().hasAll(['bio', 'skills', 'socialLinks', 'followInfoPrivate', 'profilePictureUrl', 'notificationSettings', 'lastLoginAt', 'lastLogoutAt', 'lastCheckInAt', 'status'])
        ) ||
        // Allow user to add/remove SOMEONE ELSE from their own following list
        (isAddingToList(resource.data.following, request.resource.data.following)) ||
        (isRemovingFromList(resource.data.following, request.resource.data.following)) ||
        // Group/Project ID updates
        (isAddingToList(resource.data.createdStudyGroupIds, request.resource.data.createdStudyGroupIds)) ||
        (isAddingToList(resource.data.joinedStudyGroupIds, request.resource.data.joinedStudyGroupIds)) ||
        (isAddingToList(resource.data.createdBookClubIds, request.resource.data.createdBookClubIds)) ||
        (isAddingToList(resource.data.joinedBookClubIds, request.resource.data.joinedBookClubIds)) ||
        (isAddingToList(resource.data.createdGroupProjectIds, request.resource.data.createdGroupProjectIds)) ||
        (isAddingToList(resource.data.joinedGroupProjectIds, request.resource.data.joinedGroupProjectIds)) ||
        (isAddingToList(resource.data.soloProjectIds, request.resource.data.soloProjectIds)) ||
        (isRemovingFromList(resource.data.soloProjectIds, request.resource.data.soloProjectIds)) ||
        (isAddingToList(resource.data.starredSoloProjectIds, request.resource.data.starredSoloProjectIds)) ||
        (isRemovingFromList(resource.data.starredSoloProjectIds, request.resource.data.starredSoloProjectIds))
      );
    }

    match /usernames/{username} {
      allow get: if true;
      allow list: if false;
      allow create: if request.auth.uid == request.resource.data.uid;
      allow update, delete: if false;
    }

    match /studyGroups/{groupId} {
      allow read: if true;
      allow create: if request.auth != null
                    && request.resource.data.creatorId == request.auth.uid
                    && request.resource.data.name is string
                    && request.resource.data.name.size() > 0 && request.resource.data.name.size() <= 75
                    && request.resource.data.memberIds == [request.auth.uid]
                    && request.resource.data.createdAt == request.time;
      allow update: if request.auth != null
                    && resource.data.memberIds.size() < 25
                    && request.time - resource.data.createdAt < duration.value(7, 'd')
                    && isAddingToList(resource.data.memberIds, request.resource.data.memberIds)
                    && request.resource.data.memberIds[request.resource.data.memberIds.size() - 1] == request.auth.uid;
    
      match /tasks/{taskId} {
        allow read: if true;
        allow create: if isGroupMember(groupId) 
                      && request.resource.data.creatorId == request.auth.uid
                      && request.resource.data.isCompleted == false;
        allow update: if isGroupMember(groupId);
        allow delete: if isGroupMember(groupId) && resource.data.creatorId == request.auth.uid;
      }
      
      match /checkIns/{checkInId} {
        allow read: if true;
        allow create: if isGroupMember(groupId) 
                      && request.resource.data.userId == request.auth.uid
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000
                      && request.resource.data.type in ['daily', 'weekly'];
        allow update: if isOwner(request.resource.data.userId)
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000;
        allow delete: if false;
      }
    }
    
    match /bookClubs/{bookClubId} {
      allow read: if true;
      allow create: if request.auth != null
                    && request.resource.data.creatorId == request.auth.uid
                    && request.resource.data.name is string
                    && request.resource.data.name.size() > 0 && request.resource.data.name.size() <= 75
                    && request.resource.data.topic is string
                    && request.resource.data.commitmentHours is string
                    && request.resource.data.commitmentDays is list
                    && request.resource.data.commitmentDays.size() > 0
                    && request.resource.data.memberIds == [request.auth.uid]
                    && request.resource.data.createdAt == request.time;
      allow update: if request.auth != null
                    && resource.data.memberIds.size() < 25
                    && request.time - resource.data.createdAt < duration.value(7, 'd')
                    && isAddingToList(resource.data.memberIds, request.resource.data.memberIds)
                    && request.resource.data.memberIds[request.resource.data.memberIds.size() - 1] == request.auth.uid;
    
      match /tasks/{taskId} {
        allow read: if true;
        allow create: if isBookClubMember(bookClubId) 
                      && request.resource.data.creatorId == request.auth.uid
                      && request.resource.data.isCompleted == false;
        allow update: if isBookClubMember(bookClubId);
        allow delete: if isBookClubMember(bookClubId) && resource.data.creatorId == request.auth.uid;
      }
      
      match /checkIns/{checkInId} {
        allow read: if true;
        allow create: if isBookClubMember(bookClubId) 
                      && request.resource.data.userId == request.auth.uid
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000
                      && request.resource.data.type in ['daily', 'weekly'];
        allow update: if isOwner(request.resource.data.userId)
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000;
        allow delete: if false;
      }
    }

    match /groupProjects/{groupProjectId} {
      allow read: if true;
      allow create: if request.auth != null
                    && request.resource.data.creatorId == request.auth.uid
                    && request.resource.data.name is string
                    && request.resource.data.name.size() > 0 && request.resource.data.name.size() <= 75
                    && request.resource.data.memberIds == [request.auth.uid]
                    && request.resource.data.createdAt == request.time;
      allow update: if request.auth != null
                    && resource.data.memberIds.size() < 25
                    && request.time - resource.data.createdAt < duration.value(7, 'd')
                    && isAddingToList(resource.data.memberIds, request.resource.data.memberIds)
                    && request.resource.data.memberIds[request.resource.data.memberIds.size() - 1] == request.auth.uid;

      match /tasks/{taskId} {
        allow read: if true;
        allow create: if isGroupProjectMember(groupProjectId)
                      && request.resource.data.creatorId == request.auth.uid
                      && request.resource.data.isCompleted == false;
        allow update: if isGroupProjectMember(groupProjectId);
        allow delete: if isGroupProjectMember(groupProjectId) && resource.data.creatorId == request.auth.uid;
      }

      match /checkIns/{checkInId} {
        allow read: if true;
        allow create: if isGroupProjectMember(groupProjectId)
                      && request.resource.data.userId == request.auth.uid
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000
                      && request.resource.data.type in ['daily', 'weekly'];
        allow update: if isOwner(request.resource.data.userId)
                      && request.resource.data.content is string
                      && request.resource.data.content.size() > 0 && request.resource.data.content.size() <= 2000;
        allow delete: if false;
      }
    }
    
    match /soloProjects/{soloProjectId} {
      allow read: if true;
      allow create: if request.auth != null
                    && request.resource.data.userId == request.auth.uid
                    && request.resource.data.name is string && request.resource.data.name.size() > 0 && request.resource.data.name.size() <= 100
                    && request.resource.data.projectUrl is string && request.resource.data.projectUrl.size() > 0
                    && request.resource.data.description is string && request.resource.data.description.size() <= 2000;
      allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid;
    }

    match /users/{userId}/notifications/{notificationId} {
      allow read, write: if isOwner(userId);
    }

    match /feedback/{feedbackId} {
      allow read: if false;
      allow write: if request.auth != null
                    && request.resource.data.userId == request.auth.uid
                    && request.resource.data.feedback is string
                    && request.resource.data.feedback.size() > 0
                    && request.resource.data.feedback.size() <= 2000;
    }

    match /supportRequests/{supportRequestId} {
      allow read: if false; // Should only be readable by admins, which is handled server-side
      allow create: if request.auth != null
                     && request.resource.data.userId == request.auth.uid
                     && request.resource.data.subject is string
                     && request.resource.data.subject.size() > 0 && request.resource.data.subject.size() <= 100
                     && request.resource.data.description is string
                     && request.resource.data.description.size() > 0 && request.resource.data.description.size() <= 2000
                     && request.resource.data.status == 'open';
    }
  }
}

    